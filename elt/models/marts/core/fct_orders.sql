{#
  注文ファクト。1 行 = 1 注文。売上/AOV/返金/チャネル/地理の分析基点。
#}

select
    order_id,
    order_name,
    customer_id,
    order_date,
    source_name,
    financial_status,
    fulfillment_status,
    cancel_reason,
    is_cancelled,
    has_refund,
    is_test,
    taxes_included,
    tax_exempt,
    confirmation_number,
    po_number,
    customer_locale,
    customer_accepts_marketing,
    currency_code,
    ship_country,
    ship_province,
    line_count,
    total_quantity,
    discount_code_count,
    first_discount_code,
    refund_count,
    refund_amount_total,
    fulfillment_count,
    last_fulfilled_at,
    transaction_count,
    captured_amount,
    subtotal_price,
    total_discounts,
    total_tax,
    total_shipping,
    total_refunded,
    total_price,
    current_total_price,
    net_payment,
    net_revenue,
    created_at,
    processed_at
from {{ ref('int_orders__enriched') }}
