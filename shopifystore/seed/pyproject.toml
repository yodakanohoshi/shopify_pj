[project]
name = "shopify-store-seed"
version = "0.1.0"
description = "Shopify 開発ストアへ商品・顧客・割引・注文を投入するシードスクリプト"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
